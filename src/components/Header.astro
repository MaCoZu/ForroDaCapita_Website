---
import { Image } from 'astro:assets'
import SofiaIcon from '../assets/icons/sofiaIcon2.png'
import Logo from './ForroDaCapita_Logo.astro'
import Menu from './Menu.astro'
import MobileHeader from './MobileHeader.astro'
---

<header class="w-full py-1 md:pt-8">
  <!-- Desktop layout -->
  <div class="hidden md:flex flex-row items-top justify-between mx-auto">
    <Logo />

      <Menu containerClasses="md:mt-4" />

      <!-- Desktop image -->
      <div
        class="tooltip tooltip-bottom tooltip-info"
        style="--tooltip-offset: 0.01rem;"
        data-tip="Change theme"
      >
        <Image
          src={SofiaIcon}
          alt="Toggle dark/light theme"
          class="md:w-38 md:h-38 mt-8 items-center justify-center cursor-pointer"
          id="theme-switch-img"
          role="button"
          tabindex="0"
        />
      </div>
    </div>

    <!-- Mobile layout -->
    <div
      class="md:hidden flex flex-row items-center justify-between mx-auto my-4"
    >
      <MobileHeader containerClasses="" />
    </div>
  </div>
</header>
